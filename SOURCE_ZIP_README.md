# STEM organizer — source upload set (v1.0.8)

Shippable app source for bascurtiz/STEM-organizer.

Default ML: onnxruntime-gpu (CUDA EP). Models from STEM-organizer-models tag `models`.
Rename audition uses sounddevice (same as STEM Player); ffmpeg/ffprobe for waveforms.
